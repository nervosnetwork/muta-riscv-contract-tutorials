# bank contract

```
# run a huobi-chain locally first
cd /path/to/huobi-chain
cargo run

# build the contract binary
make bin_docker

# deploy and test the chain with nodejs script
# install dependencies
yarn
node riscv_demo.js
```